# ============================================================================
# Agentic OCR Framework — Dependencies
# ============================================================================
# Tested with Python 3.13 + uv on Linux (CUDA 12.x).
#
# Install with uv (recommended):
#   uv add -r requirements.txt
#
# Or with pip:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.3.0
# Python 3.13 requires torchvision>=0.20.0 (no cp313 wheels exist for 0.18/0.19).
# peft>=0.14.0 already fixed the InterpolationMode import error independently,
# so no upper bound on torchvision is needed.
torchvision>=0.20.0
transformers>=4.49.0
peft>=0.14.0                  # 0.14+ fixes the torchvision InterpolationMode import error
bitsandbytes>=0.43.0          # 4-bit quantisation for consumer GPUs
accelerate>=0.30.0
sentencepiece>=0.2.0          # Qwen tokeniser
qwen-vl-utils>=0.0.8          # Required for Qwen2.5-VL vision token processing

# ── Vision ───────────────────────────────────────────────────────────────────
Pillow>=10.3.0
PyMuPDF>=1.24.0               # fitz — PDF rasterisation
opencv-python-headless>=4.9.0

# ── Evaluation ───────────────────────────────────────────────────────────────
jiwer>=3.0.4                  # CER / WER calculation
diff-match-patch>=20230426    # Semantic diff generation
python-Levenshtein>=0.25.1    # Edit distance for confusion matrix

# ── Data / Typing ────────────────────────────────────────────────────────────
pydantic>=2.7.0
numpy>=1.26.0

# ── UI ───────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
watchdog>=4.0.0               # Streamlit file watcher

# ── Logging / Config ─────────────────────────────────────────────────────────
python-dotenv>=1.0.1
rich>=13.7.1                  # Pretty console logging
symspellpy>=6.7.0             # Word segmentation for lexical post-processing

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0